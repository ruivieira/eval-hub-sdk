# Server dependencies (install first to avoid conflicts)
fastapi==0.110.0
uvicorn[standard]==0.27.0

# Core ML dependencies with pinned versions (minimal for demo)
torch==2.1.0
numpy==1.24.4

# LightEval framework (basic installation for demo)
lighteval==0.13.0
